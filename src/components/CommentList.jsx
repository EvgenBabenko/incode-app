import React from 'react';

export default (props) => {
  // const { taskDetails: { title, description } } = props;
  console.log(1, props);


  return (
    <React.Fragment>
      <h2>
        Comments
      </h2>
      {}
    </React.Fragment>
  );
};
